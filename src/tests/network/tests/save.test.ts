describe("Network Save", () => {

  // PASSING
  // Save (print) weights and biases
  // No intialization should have 0's in all fields
  // Initialization but no training will just have random garbage

  // FAILING
  // This should theorhetically never fail

  test("Dummy", () => {
    // Arrange
    let x = 1;
    let y = 2;

    // Act
    let sum = x + y;

    // Assert
    expect(sum).toBe(3);

  })
})