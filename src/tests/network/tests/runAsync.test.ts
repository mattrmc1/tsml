describe("Network Run Async", () => {

  // PASSING
  // Simple
  // Complex

  // FAILING
  // • Not initialized
  // Invalid input layer
  // Mix/match simple and complex inputs
  
  test("Dummy", () => {
    // Arrange
    let x = 1;
    let y = 2;

    // Act
    let sum = x + y;

    // Assert
    expect(sum).toBe(3);

  })
})