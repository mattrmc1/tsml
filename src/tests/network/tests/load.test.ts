describe("Network Load", () => {

  // PASSING
  // Load weights and biases
  // Load weights only
  // Load biases only

  // FAILING
  // Invalid weights dimensions
  // Invalid biases dimensions

  test("Dummy", () => {
    // Arrange
    let x = 1;
    let y = 2;

    // Act
    let sum = x + y;

    // Assert
    expect(sum).toBe(3);

  })
})